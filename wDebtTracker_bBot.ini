import os
import sqlite3
import json
from datetime import datetime, timedelta
from aiogram import Bot, Dispatcher, types
from aiogram.contrib.fsm_storage.memory import MemoryStorage
from aiogram.dispatcher import FSMContext
from aiogram.dispatcher.filters.state import State, StatesGroup
import pycountry
import requests
from aiogram.types import ReplyKeyboardMarkup, ReplyKeyboardRemove, InlineKeyboardMarkup, InlineKeyboardButton

# Настройка бота
API_TOKEN = os.getenv('8120127011:AAH7-NPLVH5_jW6CEMRkJXhAn1FXhr1yR24KEN')
bot = Bot(token=API_TOKEN)
storage = MemoryStorage()
dp = Dispatcher(bot, storage=storage)

# Классы состояний
class MergeDebts(StatesGroup):
    waiting_for_comment = State()
    waiting_for_confirm = State()

class AddDebt(StatesGroup):
    waiting_for_currency = State()

# Подключение к БД
def init_db():
    conn = sqlite3.connect('debts.db')
    cursor = conn.cursor()
    
    # Таблица долгов
    cursor.execute('''
    CREATE TABLE IF NOT EXISTS debts (
        id INTEGER PRIMARY KEY,
        creditor_id INTEGER,
        debtor_name TEXT,
        debtor_username TEXT,
        amount REAL,
        currency TEXT,
        category TEXT,
        created_at TEXT,
        is_paid BOOLEAN DEFAULT 0
    )
    ''')
    
    # Таблица объединенных долгов
    cursor.execute('''
    CREATE TABLE IF NOT EXISTS merged_debts (
        id INTEGER PRIMARY KEY,
        creditor_id INTEGER,
        debtor_name TEXT,
        debtor_username TEXT,
        total_amount REAL,
        currency TEXT,
        comment TEXT,
        created_at TEXT,
        original_debts TEXT
    )
    ''')
    
    # Таблица истории операций
    cursor.execute('''
    CREATE TABLE IF NOT EXISTS debt_history (
        id INTEGER PRIMARY KEY,
        debt_id INTEGER,
        action TEXT,
        amount REAL,
        currency TEXT,
        comment TEXT,
        created_at TEXT,
        FOREIGN KEY (debt_id) REFERENCES debts(id)
    )
    ''')
    
    conn.commit()
    conn.close()

init_db()

# Валидация валюты
def is_valid_currency(currency_code):
    try:
        return pycountry.currencies.get(alpha_3=currency_code) is not None
    except:
        return False

# Получение курса валют
def get_exchange_rate(from_currency, to_currency):
    API_URL = f"https://api.exchangerate-api.com/v4/latest/{from_currency}"
    try:
        response = requests.get(API_URL)
        data = response.json()
        return data['rates'][to_currency]
    except:
        return None

# Команда /start
@dp.message_handler(commands=['start'])
async def cmd_start(message: types.Message):
    await message.reply(
        "🤖 Бот для учета долгов\n\n"
        "Доступные команды:\n"
        "/add_debt - Добавить долг\n"
        "/debts - Список долгов\n"
        "/return - Вернуть долг\n"
        "/merge_debts - Объединить долги\n"
        "/stats - Статистика\n"
        "/export_excel - Экспорт в Excel\n"
        "/export_pdf - Экспорт в PDF"
    )

# Команда /add_debt
@dp.message_handler(commands=['add_debt'])
async def cmd_add_debt(message: types.Message):
    args = message.get_args().split()
    if len(args) < 3:
        await message.reply(
            "❌ Неверный формат. Используйте:\n"
            "/add_debt [имя/@юзернейм] [сумма] [валюта] [категория]\n"
            "Пример: /add_debt Иван 1000 RUB Продукты"
        )
        return
    
    debtor_input = args[0]
    try:
        amount = float(args[1])
    except ValueError:
        await message.reply("❌ Сумма должна быть числом")
        return
    
    currency = args[2].upper()
    if len(args) >= 4:
        category = ' '.join(args[3:])
    else:
        category = "Другое"
    
    if not is_valid_currency(currency):
        await message.reply(f"❌ Валюта '{currency}' не поддерживается")
        return
    
    # Сохраняем долг в БД
    conn = sqlite3.connect('debts.db')
    cursor = conn.cursor()
    
    debtor_username = debtor_input if debtor_input.startswith('@') else None
    debtor_name = debtor_input.replace('@', '')
    
    cursor.execute('''
    INSERT INTO debts 
    (creditor_id, debtor_name, debtor_username, amount, currency, category, created_at)
    VALUES (?, ?, ?, ?, ?, ?, ?)
    ''', (
        message.from_user.id,
        debtor_name,
        debtor_username,
        amount,
        currency,
        category,
        datetime.now().strftime('%Y-%m-%d %H:%M:%S')
    ))
    
    # Записываем в историю
    debt_id = cursor.lastrowid
    cursor.execute('''
    INSERT INTO debt_history 
    (debt_id, action, amount, currency, comment, created_at)
    VALUES (?, ?, ?, ?, ?, ?)
    ''', (
        debt_id,
        'create',
        amount,
        currency,
        f"Создан долг: {category}",
        datetime.now().strftime('%Y-%m-%d %H:%M:%S')
    ))
    
    conn.commit()
    conn.close()
    
    await message.reply(
        f"✅ Долг добавлен:\n"
        f"• Кто должен: {debtor_name}\n"
        f"• Сумма: {amount} {currency}\n"
        f"• Категория: {category}"
    )

# Команда /debts
@dp.message_handler(commands=['debts'])
async def cmd_debts(message: types.Message):
    conn = sqlite3.connect('debts.db')
    cursor = conn.cursor()
    
    cursor.execute('''
    SELECT debtor_name, SUM(amount), currency 
    FROM debts 
    WHERE creditor_id = ? AND is_paid = 0
    GROUP BY debtor_name, currency
    ''', (message.from_user.id,))
    
    debts = cursor.fetchall()
    conn.close()
    
    if not debts:
        await message.reply("📭 У вас нет активных долгов")
        return
    
    response = "📋 Ваши долги:\n\n"
    for debt in debts:
        response += f"• {debt[0]} - {debt[1]} {debt[2]}\n"
    
    await message.reply(response)

# Команда /return
@dp.message_handler(commands=['return'])
async def cmd_return(message: types.Message):
    args = message.get_args().split()
    if len(args) < 3:
        await message.reply(
            "❌ Неверный формат. Используйте:\n"
            "/return [имя/@юзернейм] [сумма] [валюта]\n"
            "Пример: /return Иван 500 RUB"
        )
        return
    
    debtor_input = args[0]
    try:
        amount = float(args[1])
    except ValueError:
        await message.reply("❌ Сумма должна быть числом")
        return
    
    currency = args[2].upper()
    
    conn = sqlite3.connect('debts.db')
    cursor = conn.cursor()
    
    cursor.execute('''
    SELECT id, amount 
    FROM debts 
    WHERE creditor_id = ? 
    AND (debtor_name = ? OR debtor_username = ?)
    AND currency = ?
    AND is_paid = 0
    ORDER BY created_at
    ''', (message.from_user.id, debtor_input, debtor_input, currency))
    
    debt = cursor.fetchone()
    
    if not debt:
        await message.reply("❌ Активный долг не найден")
        conn.close()
        return
    
    debt_id, debt_amount = debt
    new_amount = debt_amount - amount
    
    if new_amount <= 0:
        cursor.execute('UPDATE debts SET is_paid = 1 WHERE id = ?', (debt_id,))
        comment = "Долг полностью погашен"
    else:
        cursor.execute('UPDATE debts SET amount = ? WHERE id = ?', (new_amount, debt_id))
        comment = f"Частичное погашение. Остаток: {new_amount} {currency}"
    
    # Записываем в историю
    cursor.execute('''
    INSERT INTO debt_history 
    (debt_id, action, amount, currency, comment, created_at)
    VALUES (?, ?, ?, ?, ?, ?)
    ''', (
        debt_id,
        'return',
        amount,
        currency,
        comment,
        datetime.now().strftime('%Y-%m-%d %H:%M:%S')
    ))
    
    conn.commit()
    conn.close()
    
    await message.reply(f"✅ {comment}")

# Команда /merge_debts
@dp.message_handler(commands=['merge_debts'])
async def cmd_merge_debts(message: types.Message, state: FSMContext):
    args = message.get_args().split()
    if not args:
        await message.reply("❌ Укажите имя: /merge_debts Иван")
        return
    
    debtor_input = args[0]
    conn = sqlite3.connect('debts.db')
    cursor = conn.cursor()
    
    cursor.execute('''
    SELECT id, amount, currency, category, created_at 
    FROM debts 
    WHERE creditor_id = ? 
    AND (debtor_name = ? OR debtor_username = ?)
    AND is_paid = 0
    ''', (message.from_user.id, debtor_input, debtor_input))
    
    debts = cursor.fetchall()
    conn.close()
    
    if not debts:
        await message.reply("❌ Нет активных долгов для объединения")
        return
    
    # Проверка валют
    currencies = {debt[2] for debt in debts}
    if len(currencies) > 1:
        await message.reply(
            f"❌ Валюты не совпадают: {', '.join(currencies)}\n"
            "Используйте /convert сначала."
        )
        return
    
    await state.update_data({
        'debtor_input': debtor_input,
        'debts': debts,
        'currency': debts[0][2]
    })
    
    # Формируем список долгов
    debts_list = "\n".join(
        f"{i+1}. {debt[1]} {debt[2]} - {debt[3]} ({debt[4][:10]})"
        for i, debt in enumerate(debts)
    )
    total = sum(debt[1] for debt in debts)
    
    # Запрос комментария
    keyboard = ReplyKeyboardMarkup(resize_keyboard=True)
    keyboard.add("❌ Отмена")
    
    await message.reply(
        f"📋 Найдены долги {debtor_input}:\n{debts_list}\n\n"
        f"Общая сумма: {total} {debts[0][2]}\n\n"
        "Введите комментарий для объединения:",
        reply_markup=keyboard
    )
    await MergeDebts.waiting_for_comment.set()

# Обработка комментария для объединения
@dp.message_handler(state=MergeDebts.waiting_for_comment)
async def process_merge_comment(message: types.Message, state: FSMContext):
    if message.text == "❌ Отмена":
        await message.reply("Объединение отменено", reply_markup=ReplyKeyboardRemove())
        await state.finish()
        return
    
    data = await state.get_data()
    debts = data['debts']
    total = sum(debt[1] for debt in debts)
    
    await state.update_data({
        'comment': message.text,
        'total': total
    })
    
    # Кнопки подтверждения
    keyboard = InlineKeyboardMarkup()
    keyboard.row(
        InlineKeyboardButton("✅ Подтвердить", callback_data="confirm_merge"),
        InlineKeyboardButton("❌ Отмена", callback_data="cancel_merge")
    )
    
    await message.reply(
        f"Подтвердите объединение:\n"
        f"• Должник: {data['debtor_input']}\n"
        f"• Сумма: {total} {data['currency']}\n"
        f"• Комментарий: {message.text}",
        reply_markup=keyboard,
        reply_markup=ReplyKeyboardRemove()
    )
    await MergeDebts.waiting_for_confirm.set()

# Обработка подтверждения объединения
@dp.callback_query_handler(lambda c: c.data in ['confirm_merge', 'cancel_merge'], state=MergeDebts.waiting_for_confirm)
async def process_merge_confirmation(callback_query: types.CallbackQuery, state: FSMContext):
    if callback_query.data == 'cancel_merge':
        await callback_query.message.edit_text("❌ Объединение отменено")
        await state.finish()
        return
    
    data = await state.get_data()
    debts = data['debts']
    total = data['total']
    currency = data['currency']
    comment = data['comment']
    debtor_input = data['debtor_input']
    
    conn = sqlite3.connect('debts.db')
    cursor = conn.cursor()
    
    # Создаем объединенный долг
    cursor.execute('''
    INSERT INTO merged_debts 
    (creditor_id, debtor_name, total_amount, currency, comment, created_at, original_debts)
    VALUES (?, ?, ?, ?, ?, ?, ?)
    ''', (
        callback_query.from_user.id,
        debtor_input,
        total,
        currency,
        comment,
        datetime.now().strftime('%Y-%m-%d %H:%M:%S'),
        json.dumps([{'id': debt[0], 'amount': debt[1]} for debt in debts])
    ))
    
    merged_id = cursor.lastrowid
    
    # Помечаем исходные долги как погашенные
    for debt in debts:
        cursor.execute('UPDATE debts SET is_paid = 1 WHERE id = ?', (debt[0],))
        
        # Записываем в историю
        cursor.execute('''
        INSERT INTO debt_history 
        (debt_id, action, amount, currency, comment, created_at)
        VALUES (?, ?, ?, ?, ?, ?)
        ''', (
            debt[0],
            'merge',
            debt[1],
            currency,
            f"Объединено в долг #{merged_id}",
            datetime.now().strftime('%Y-%m-%d %H:%M:%S')
        ))
    
    conn.commit()
    conn.close()
    
    await callback_query.message.edit_text(
        f"✅ Долги объединены!\n"
        f"• Общая сумма: {total} {currency}\n"
        f"• Комментарий: {comment}"
    )
    await state.finish()

# Запуск бота
if __name__ == '__main__':
    from aiogram import executor
    executor.start_polling(dp, skip_updates=True)